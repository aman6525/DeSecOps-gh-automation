name: test
on:
 push:
  branches:
   "*"
jobs:
 build:
  name: scanautomation
  runs-on: ubuntu-latest
  steps:
  - name: Running the vulnerable-app
    run: 
      apt-get install php
  - name: Starting the zero scanner
    run: 
      echo zero-scanner
      echo output-is-here


